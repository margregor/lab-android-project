<?xml version="1.0" encoding="utf-8"?><manifest xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools">

<uses-feature
    android:glEsVersion="${GL_VERSION_CODE}"
    android:required="true" />

<!-- Indicates the requirements for this application -->

<uses-permission
    android:name="android.permission.INTERNET"
    android:required="${REQUIRE_INTERNET}" />

<uses-permission
    android:name="android.permission.VIBRATE"
    android:required="${REQUIRE_VIBRATION}" />

<!--suppress AndroidDomInspection because it can't read manifestPlaceholders-->
<uses-feature
    android:name="android.hardware.sensor.gyroscope"
    android:required="${REQUIRE_GYROSCOPE}" />

<!--suppress AndroidDomInspection because it can't read manifestPlaceholders-->
<uses-feature
    android:name="android.hardware.sensor.accelerometer"
    android:required="${REQUIRE_ACCELEROMETER}" />

<!-- Application config -->

<application
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    tools:targetApi="31">

    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.Labap">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <activity
        android:name=".NativeLoader"
        android:resizeableActivity="true"
        android:configChanges="screenLayout|density|uiMode|orientation|smallestScreenSize|keyboardHidden|screenSize"
        android:launchMode="singleTask"
        android:clearTaskOnLaunch="true"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
        android:exported="false">

        <meta-data
            android:name="android.app.lib_name"
            android:value="${APP_LIB_NAME}" />
    </activity>



</application>

</manifest>

